Drop your pixel art here so the Belong prototype can pick it up.

Currently referenced assets:
- backgrounds/menu_background.png — menu and overlay backdrop
- backgrounds/outsideschool.png — default scene art for both characters
- characters/anxious_maya.png — Maya portrait + character select card
- characters/crying_maya.png — Reserved for future emotional state swaps

Optional future additions:
- Alex portraits (characters/alex_*.png)
- Scene-specific backgrounds (backgrounds/maya_*.png, backgrounds/alex_*.png)
- UI tiles (ui/*.png) for custom button textures

Add new files with matching names and update `js/characters.js` or `js/scenarios.js` to wire them in.
